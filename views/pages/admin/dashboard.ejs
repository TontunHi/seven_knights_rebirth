<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../../partials/head') %>
    <style>
        body { 
            background-color: #0f0f0f; 
            font-family: 'Segoe UI', sans-serif;
        }

        /* --- Modern Navbar --- */
        .navbar-modern {
            height: 64px; 
            background: rgba(18, 18, 18, 0.95); 
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1); 
            position: fixed; top: 0; left: 0; right: 0; z-index: 1000;
            padding: 0 20px; 
            display: flex; align-items: center; justify-content: space-between;
            box-shadow: 0 4px 20px rgba(0,0,0,0.4);
        }
        
        .nav-brand-box { display: flex; align-items: center; gap: 12px; }
        .site-logo { height: 40px; width: auto; object-fit: contain; filter: drop-shadow(0 0 5px rgba(255,193,7,0.3)); }
        .nav-title { font-weight: 700; color: #fff; font-size: 1.1rem; letter-spacing: 0.5px; }
        
        .nav-btn { 
            background: rgba(255, 255, 255, 0.05); color: #ccc; 
            border: 1px solid rgba(255, 255, 255, 0.1); 
            padding: 6px 16px; border-radius: 20px; 
            font-size: 0.85rem; text-decoration: none; 
            display: inline-flex; align-items: center; gap: 6px; 
            transition: all 0.2s;
        }
        .nav-btn:hover { background: rgba(255, 255, 255, 0.1); color: #fff; transform: translateY(-1px); }

        /* --- Dashboard Cards --- */
        .dashboard-container {
            margin-top: 100px;
            max-width: 1200px;
            margin-left: auto;
            margin-right: auto;
            padding-bottom: 50px;
        }

        .dash-card {
            background: #1e1e1e;
            border: 1px solid #333;
            border-radius: 12px;
            height: 100%;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        .dash-card:hover {
            transform: translateY(-5px);
            border-color: #555;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }
        
        .card-icon {
            font-size: 3rem;
            margin-bottom: 15px;
            background: -webkit-linear-gradient(#ffc107, #ff8c00);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .card-body { padding: 30px; text-align: center; }
        .card-title { color: #fff; font-weight: 700; margin-bottom: 10px; }
        .card-desc { color: #888; font-size: 0.9rem; margin-bottom: 25px; min-height: 40px; }

        .btn-action {
            display: block; width: 100%;
            padding: 10px; margin-bottom: 8px;
            border-radius: 8px; font-weight: 600; font-size: 0.9rem;
            text-decoration: none; transition: 0.2s;
        }
        
        .btn-outline-gold { border: 1px solid #ffc107; color: #ffc107; }
        .btn-outline-gold:hover { background: #ffc107; color: #000; }
        
        .btn-outline-white { border: 1px solid #444; color: #ccc; }
        .btn-outline-white:hover { background: #333; color: #fff; border-color: #666; }

        .btn-outline-purple { border: 1px solid #d2ade6; color: #d2ade6; }
        .btn-outline-purple:hover { background: #d2ade6; color: #000; }

    </style>
</head>
<body class="text-light">
    
    <nav class="navbar-modern">
        <div class="nav-brand-box">
            <img src="/images/about_website/logo_seven_knight_rebirth.png" alt="Logo" class="site-logo">
            <span class="nav-title">Admin Dashboard</span>
        </div>
        <div class="d-flex align-items-center gap-3">
            <a href="/" target="_blank" class="nav-btn">
                <i class="bi bi-globe"></i> View Website
            </a>
            <a href="/admin/logout" class="nav-btn">
                <i class="bi bi-box-arrow-right"></i> Logout
            </a>
        </div>
    </nav>

    <div class="container dashboard-container">
        <div class="row g-4">
            
            <div class="col-md-4">
                <div class="dash-card">
                    <div class="card-body">
                        <i class="bi bi-hammer card-icon"></i>
                        <h4 class="card-title">Build Manager</h4>
                        <p class="card-desc">Manage hero equipment builds, stats, and accessories.</p>
                        
                        <a href="/admin/manage/build/legendary" class="btn-action btn-outline-gold">
                            Legendary Heroes
                        </a>
                        <a href="/admin/manage/build/rare" class="btn-action btn-outline-white">
                            Rare Heroes
                        </a>
                    </div>
                </div>
            </div>

            <div class="col-md-4">
                <div class="dash-card">
                    <div class="card-body">
                        <i class="bi bi-trophy-fill card-icon"></i>
                        <h4 class="card-title">Tier List</h4>
                        <p class="card-desc">Update rankings for PVP, PVE, and Pets with drag & drop.</p>
                        
                        <a href="/admin/manage/tierlist?category=PVP" class="btn-action btn-outline-gold">
                            Manage Tier List
                        </a>
                    </div>
                </div>
            </div>

            <div class="col-md-4">
                <div class="dash-card">
                    <div class="card-body">
                        <i class="bi bi-map-fill card-icon" style="-webkit-text-fill-color: #d2ade6;"></i>
                        <h4 class="card-title">Comp & Guide</h4>
                        <p class="card-desc">Manage team formations for Stage, Nightmare, and Raids.</p>
                        
                        <a href="/admin/manage/comp/stage" class="btn-action btn-outline-purple">
                            <i class="bi bi-flag-fill me-2"></i>Stage & Nightmare
                        </a>
                        
                        <button class="btn-action btn-outline-white opacity-50" disabled>
                            Raid (Coming Soon)
                        </button>
                    </div>
                </div>
            </div>

        </div>
    </div>

    <%- include('../../partials/footer') %>
</body>
</html>