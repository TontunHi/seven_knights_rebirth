# Dependencies
node_modules/
npm-debug.log
yarn-error.log
pnpm-debug.log

# Environment Variables (IMPORTANT: Don't commit secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Database (SQLite)
# เรามักจะไม่ Commit ไฟล์ Database binary ขึ้น Git เพื่อป้องกัน Conflict
# แต่จะให้ระบบสร้างไฟล์ใหม่เอง หรือใช้ Seed data แทน
*.db
*.sqlite
*.sqlite3
database/*.db

# OS System Files
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# IDE / Editors
.vscode/
.idea/
*.swp
*.swo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional: Uploaded Images
# ถ้าคุณมองว่ารูป Hero/Formation เป็น Static Content (ข้อมูลเกม) ที่ต้องมีติดเว็บ -> ไม่ต้อง Uncomment บรรทัดล่าง
# แต่ถ้ารูปพวกนี้ถือเป็น User Generated Content (ที่ Admin อัปโหลดใหม่เรื่อยๆ ตอนรันจริง) -> ให้ลบ # ออก
# public/images/heroes/
# public/images/formation/
# public/images/about_website/
# public/images/logo_tiers/